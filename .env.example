OPENAI_API_KEY=sk-...
# Primary (cheap, fast) model for text-only extraction
OPENAI_MODEL=gpt-4o-mini
# Fallback model used for Vision / text-only retries when core fields are missing
OPENAI_FALLBACK_MODEL=gpt-4o
